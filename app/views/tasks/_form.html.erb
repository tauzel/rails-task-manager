<div class="container">
  <%= simple_form_for(@task) do |f| %>
    <%= f.input :title %>
    <%= f.input :details %>
    <%= f.submit :submit, class: 'btn btn-primary mt-3' %>
  <% end %>
  <%= link_to 'Go back', tasks_path, class: "btn btn-primary mt-3"%>
</div>
